version: 0.2
phases:
  install:
    commands:
      - echo "install phase" 
  pre_build:
    commands:
      - echo "pre-build phase" 
  build:
    commands:
      - echo "build phase"
      - ls -lpa .
  post_build:
    commands:
      - echo "post build phase"
